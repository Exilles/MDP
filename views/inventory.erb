<!DOCTYPE html>
<html>
<head>
    <meta charset='UTF-8' />
    <title>Inventory</title>
</head>
<body>
<h1>Inventory</h1>
<ul>
    <% @items.each do |item| %>
        <li>
            <a href="/inventory/<%= item.id %>"><%= item.name %></a>
        </li>
    <% end %>
</ul>
<a href="/inventory/new">Add New Item</a>
</body>
</html>